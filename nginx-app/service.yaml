apiVersion: v1
kind: Service
metadata:
  name: nginx-gitops-service
  namespace: default
  labels:
    app: nginx-gitops
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx-gitops
